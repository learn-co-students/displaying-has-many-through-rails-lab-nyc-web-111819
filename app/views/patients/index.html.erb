<h2>List of Patients</h2>

<ul>
<% @patients.each do |patient| %>

    <li><%= link_to "Name: #{patient.name} Number of Appointments: #{patient.appointments.length} ", patient_path(patient) %></li>

<% end %>
</ul>

<%= button_to "Add Patient", new_patient_path, method: :get %>